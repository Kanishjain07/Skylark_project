# Python deps for the Vercel serverless function (api/index.py).
# uvicorn is intentionally omitted — Vercel serves the ASGI app directly.
fastapi>=0.110
httpx>=0.27
openai>=1.50
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
python-dateutil>=2.9
